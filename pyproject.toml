[project]
name = "docker-mlflow"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "boto3==1.37.28",
    "mlflow==2.20.3",
    "psycopg2-binary==2.9.10",
]

[tool.uv]
dev-dependencies = [
    "python-dotenv==1.1.0",
    "scikit-learn==1.6.1",
]
