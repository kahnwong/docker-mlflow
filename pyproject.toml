[project]
name = "docker-mlflow"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "boto3==1.39.8",
    "mlflow==2.22.1",
    "psycopg2-binary==2.9.10",
]

[tool.uv]
dev-dependencies = [
    "scikit-learn==1.7.1",
]
